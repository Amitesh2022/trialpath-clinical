// @vitest-environment jsdom
import{render,screen}from'@testing-library/react';import{MemoryRouter}from'react-router-dom';import{QueryClient,QueryClientProvider}from'@tanstack/react-query';import{describe,expect,it}from'vitest';import App from'./App';describe('TrialPath',()=>{it('renders clinical operations',async()=>{render(<QueryClientProvider client={new QueryClient()}><MemoryRouter><App/></MemoryRouter></QueryClientProvider>);expect(screen.getByRole('heading',{name:'Clinical operations overview'})).toBeTruthy();expect(await screen.findByText('Quality coverage')).toBeTruthy();expect(screen.getByText('Saint-Luc Research Centre')).toBeTruthy()})})
